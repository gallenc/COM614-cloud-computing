apiVersion: networking.k8s.io/v1beta1
kind: ingress
metadata:
  name: dashboard-ingress
  namesapce: kubernetes-dashboard
spec:
  rules:
  - host: kubernetes-dashboard.com
    http:
      paths:
      - backend:
          serviceName: kubernetes-dashboard
          servicePort: 80
  
