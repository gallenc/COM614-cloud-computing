---
- name: stop apache2
  systemd:
     name: apache2
     enabled: false
     state: stopped
        
- name: Uninstall apache web server on debian/ubuntu
  apt:
    pkg: apache2
    state: absent
    update_cache: true
        
