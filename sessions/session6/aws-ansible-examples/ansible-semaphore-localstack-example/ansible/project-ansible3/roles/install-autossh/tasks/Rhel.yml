---
- name: Install apache web server on RHEL
  yum:
    name: autossh
    state: latest
    update_cache: true

# cant work with no systemd in container
#- name: Enable and start the apache2
#  systemd:
#    name: httpd
#    enabled: true
#    state: started