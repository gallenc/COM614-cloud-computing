---
- name: stop apache2
  systemd:
     name: autossh
     enabled: false
     state: stopped
        
- name: Uninstall apache web server on debian/ubuntu
  apt:
    pkg: autossh
    state: absent
    update_cache: true
        
